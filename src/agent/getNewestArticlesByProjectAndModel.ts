// from supabase
// load the active (non archived) prompts for the project
// load the model you want to run on (from where?)
// load the (100) latest articles that don't have judgment by all prompts
